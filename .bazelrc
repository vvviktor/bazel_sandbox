build --incompatible_enable_cc_toolchain_resolution             # allow bazel to use custom toolchains
build --define=MINGW_PATH="C:/msys64/ucrt64"                    # var to set the correct local toolchain path 
build --define=GCC_VERSION="13.2.0"                             # var to set the correct toolchain GCC version
